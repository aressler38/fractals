

<VirtualHost *:80>

  ServerName fractals.localhost
  ServerAlias fractals.localhost
  #DocumentRoot "/home/vagrant/fractals"
  DocumentRoot "${FRACTALS_WEB_ROOT}"

  <Directory "${FRACTALS_WEB_ROOT}">
  #<Directory "/home/vagrant/fractals">
    Options +Indexes +FollowSymLinks
    Require all granted
  </Directory>


</VirtualHost>
