

<VirtualHost *:80>

  ServerName fractals.localhost
  ServerAlias fractals.localhost
  DocumentRoot "${FRACTALS_WEB_ROOT}"

  <Directory "${FRACTALS_WEB_ROOT}">
    Options +Indexes +FollowSymLinks
    Require all granted
  </Directory>


</VirtualHost>
